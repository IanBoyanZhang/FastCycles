## 

`bitmasking dispatchers`

The implication of 

`t the n & (n - 1)` finds power of 2

C++ 20

`n & -n` lowest set bit (the rightmost '1' bit) of an integer

`-n` two's complement (~n) and then adding 1

a more universal version stl library in C++20 is 

`[has_single_bit](https://en.cppreference.com/cpp/numeric/has_single_bit)`

Which is related to `popcount`

